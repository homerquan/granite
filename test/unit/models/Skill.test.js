"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Skill');
const Fixture = require('../../fixtures/SkillFixture');

describe('models:Skill', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
