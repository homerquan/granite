"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Conversation');
const Fixture = require('../../fixtures/ConversationFixture');

describe('models:Conversation', () => {
  it('Should be tested', () => {
    assert(true);
  });
});
