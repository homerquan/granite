"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Bot');
const Fixture = require('../../fixtures/BotFixture');

describe('models:Bot', () => {
  it('Should be tested', () => {
    assert(true);
  });
});
