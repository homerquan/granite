"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Visitor');
const Fixture = require('../../fixtures/VisitorFixture');

describe('models:Visitor', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
