"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Client');
const Fixture = require('../../fixtures/ClientFixture');

describe('models:Client', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
