"use strict";

const assert = require('chai').assert;
const Model = require('../../../api/models/Message');
const Fixture = require('../../fixtures/MessageFixture');

describe('models:Message', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
