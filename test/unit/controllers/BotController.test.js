"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/BotController');

describe('controllers:BotController', () => {
  it('Should be tested', () => {
    assert(true);
  });
});
