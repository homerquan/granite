"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/ConversationController');

describe('controllers:ConversationController', () => {
  it('Should be tested', () => {
    assert(true);
  });
});
