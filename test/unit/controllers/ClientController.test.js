"use strict";

const assert = require('chai').assert;
const Controller = require('../../../api/controllers/ClientController');

describe('controllers:ClientController', () => {
  it('Should be tested', () => {
    assert(true);
  });
});
