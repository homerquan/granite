"use strict";

const assert = require('chai').assert;
const sinon = require('sinon');
const Policy = require('../../../api/policies/isOwner');

describe('policies:isOwner', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
