"use strict";

const assert = require('chai').assert;
const sinon = require('sinon');
const Policy = require('../../../api/policies/isAdmin');

describe('policies:isAdmin', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
