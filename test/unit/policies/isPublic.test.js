"use strict";

const assert = require('chai').assert;
const sinon = require('sinon');
const Policy = require('../../../api/policies/isPublic');

describe('policies:isPublic', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
