"use strict";

const assert = require('chai').assert;
const sinon = require('sinon');
const Policy = require('../../../api/policies/isLoggedIn');

describe('policies:isLoggedIn', () => {
  it('Should be tested', () => {
    assert(false);
  });
});
